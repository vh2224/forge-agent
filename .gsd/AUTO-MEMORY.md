<!-- gsd-auto-memory | project: forge-agent | extraction_count: 50 -->
<!-- ranked by: confidence × (1 + hits × 0.1) | cap: 50 active -->

## Gotcha
- [MEM001] (gotcha) confidence:0.95 hits:3 — PostCompact hook writes compact-signal.json, not PreCompact. Compaction happens mid-context and STATE is stale; writing the signal after compaction completes ensures recovery machinery sees it after state has been reloaded. PostCompact must re-read auto-mode.json from disk (no in-memory state available). Now live in merge-settings.js LIFECYCLE_HOOKS. Signal is deleted immediately after recovery check to prevent re-triggering.
  source: discuss-milestone/M001 → execute-task/T01-T03 | updated: 2026-04-15

- [MEM047] (gotcha) confidence:0.95 hits:2 — YAML regex anchoring: `\s*` after colon matches newlines, causing greedy cross-line capture in `extractTopLevelValue(yaml, "key")`. Pattern `/^key:\s*(...)$/m` captures `"- x.js"` on next line as part of value. Fix: use `[ \t]*` (space/tab only) to restrict to current line. This pattern is critical for all frontmatter/YAML parsing in Forge, including S02 file-audit, S03 schema-verifier, S04 plan-checker downstream consumers of forge-must-haves.js. Validated in S01 smoke tests across legacy/valid/malformed fixtures — pattern is authoritative.
  source: execute-task/T01 → complete-slice/S01 | updated: 2026-04-16

- [MEM064] (gotcha) confidence:0.93 hits:2 — file_audit.ignore_list key name is LOCKED — forge-completer T04 consumer parses this exact key via regex. The key cannot be renamed or refactored without updating the completer agent instructions and re-validating the regex pattern. Non-negotiable configuration contract for S02/S03 audit pipeline. Confirmed implemented in T04 sub-step 1.6 prefs cascade.
  source: execute-task/T05-M003 → execute-task/T04-M003 → complete-slice/S02 | updated: 2026-04-16

- [MEM017] (gotcha) confidence:0.92 hits:2 — $ARGUMENTS magic variable is command-only. Skills receive arguments via Skill({args:"..."}) parameter and must parse from that, NOT from $ARGUMENTS. Shims forwarding commands to skills must explicitly pass args parameter; skill body never sees $ARGUMENTS directly. Mismatch causes lost arguments in skill invocation. Confirmed: forge-auto and forge-new-milestone shims both use Skill({skill:"name", args:"$ARGUMENTS"}) pattern.
  source: research-slice/S03 → execute-task/T02-S03 → execute-task/T04-S03 | updated: 2026-04-15

- [MEM046] (gotcha) confidence:0.90 hits:2 — Executor schema validation gate (Step 1a) runs BEFORE `status: RUNNING` via shell-out to `forge-must-haves.js --check`. Branches on legacy/valid/malformed outcomes; legacy plans pass with `legacy_schema: true` warn note, malformed plans block with `scope_exceeded`. Inserted as Step 1a (between Step 1 read-plan and Step 2 read-standards) to preserve ordering invariant — no file writes happen until validation passes. Summary Format documents `legacy_schema` optional field. Same ordering rule applies to all future validation gates (S03 verifier, S04 plan-checker): check before any state mutation.
  source: plan-slice/S01 → execute-task/T03 → complete-slice/S01 | updated: 2026-04-16

- [MEM052] (gotcha) confidence:0.95 hits:1 — Claude Code PostToolUse hook `tool_response` does NOT expose `exit_code` directly for Bash. Only `stdout`, `stderr`, and `interrupted` boolean are available. For Bash, derivable exit status is `ok:boolean` (inverse of `interrupted` + stderr presence). The executor's `verification_evidence:` self-reports `exit_code: N` from its own context state; the completer cross-refs `command` string match only, not exit codes. Evidence hook writes `ok:boolean` to the JSONL log; executor's frontmatter carries its own exit codes separately. PostToolUse hooks ALWAYS spawn as subprocess — wall-clock timing includes Node.js cold-start (40-80ms Windows Defender). Report BOTH in-script work time (process.hrtime.bigint, typically 2-5ms) AND full wall-clock external measurement (averaged ≥50 runs, typically 60-130ms Windows). Perf budget is "additional cost on top of baseline hook" — not absolute end-to-end time.
  source: plan-slice/S02 → research-slice/S02 | updated: 2026-04-16

- [MEM050] (gotcha) confidence:0.93 hits:1 — `expected_output:` is a **top-level YAML frontmatter key** (sibling to `must_haves:`, not nested inside it). Downstream consumers like S02 file-audit read `parseMustHaves().expected_output` as array of path strings. Missing this structure causes consumers to miss the output list or treat it as part of the `must_haves:` object. Lock documented in S01-SUMMARY.md forward intelligence and KEY_DECISIONS: confirmed non-negotiable position for schema contract. Validated in structured-valid-plan.md fixture.
  source: complete-slice/S01 | updated: 2026-04-16

- [MEM051] (gotcha) confidence:0.93 hits:1 — `parseMustHaves(content)` throws `Error("malformed must_haves schema: <field> — <reason>")` on invalid frontmatter shape. Downstream consumers (S02, S03, S04) must wrap calls in try/catch. If catch occurs, safely treat as `legacy:true` and skip structured validation. No partial result returned on exception — either valid structured schema or thrown error. S01 T01 implementation confirmed: all three smoke fixtures tested (legacy pass, valid pass, malformed throws with field-specific message). Error contract is explicit and durable.
  source: complete-slice/S01 | updated: 2026-04-16

- [MEM057] (gotcha) confidence:0.93 hits:1 — T04 file-audit fallback chain for git base: try `master`, then `main`, then `git rev-parse --abbrev-ref origin/HEAD`, then fall back to working-tree diff (same as auto_commit:false mode). Hardcoding `master` fails on repos using `main` or with detached HEAD. Wrap in try/catch silent-fail per MEM008 — advisory section omitted rather than blocking the complete. Auto-detect via `git symbolic-ref refs/remotes/origin/HEAD` → strip `refs/remotes/origin/` prefix. Implemented in T04 sub-step 1.6.
  source: research-slice/S02 → execute-task/T04-M003 | updated: 2026-04-16

- [MEM066] (gotcha) confidence:0.93 hits:1 — File Audit is advisory-only in forge-completer: never blocks merge or alters status: code. Missing or unexpected files are documented in S##-SUMMARY.md `## File Audit` section (if non-empty), but both missing and unexpected lists are informational. Failures in git/node shelling (try/catch) are silently omitted from advisory. This preserves S02 `status: done` semantics — audit does not escalate to `partial` or `blocked`. Design intent: provide visibility into file discrepancies without breaking slice/milestone closures.
  source: execute-task/T04-M003 → complete-slice/S02 | updated: 2026-04-16

- [MEM008] (gotcha) confidence:0.92 hits:1 — Hook errors (file I/O, JSON parse, disk full) must be caught and swallowed via try/catch. Hooks cannot crash Claude Code. This includes PostCompact handler in merge-settings.js and evidence-write block in PostToolUse — all file operations are wrapped, and silent failures are preferable to terminating the context. T01 confirms: evidence write failed gracefully on unwritable cwd (hook exit 0).
  source: complete-slice/S01 → execute-task/T01-M003 | updated: 2026-04-16

- [MEM055] (gotcha) confidence:0.90 hits:1 — Task execution order in S02 must be strictly sequential: T01 → T02 → T05 → T04 → T03. Rationale: T03 and T04 both edit `agents/forge-completer.md` in different steps (T03 writes `## Evidence Flags` logic, T04 writes `## File Audit` logic to same agent file). Sequential execution prevents merge conflicts. T05 placed before T04 so prefs defaults are live when T04 agent doc references them. Non-negotiable ordering constraint for this slice.
  source: plan-slice/S02 | updated: 2026-04-16

- [MEM067] (gotcha) confidence:0.95 hits:0 — PostToolUse hooks in Claude Code spawn as subprocess with wall-clock timing that includes Node.js cold-start (40–80 ms Windows Defender). Perf budget semantics distinguish two measurements: in-script work time (process.hrtime.bigint, typically 2–5 ms for file append) vs. external wall-clock measurement averaged ≥50 runs (typically 60–130 ms on Windows). Executor reports "additional cost on top of baseline hook" — not absolute end-to-end time. Hooks cannot measure their own wall-clock overhead; measurement must be external benchmarking. Documented in T01 verification and applies to all future PostToolUse implementations (evidence write, future audit hooks). Non-negotiable for perf budgeting of hook-based observability.
  source: research-slice/S02 → execute-task/T01-M003 → complete-slice/S02 | updated: 2026-04-16

- [MEM068] (gotcha) confidence:0.95 hits:0 — Agent edits in source repo require install.sh re-run before taking effect. Agents are installed into `~/.claude/agents/` on first `install.sh` run; subsequent agent runs load from installed copy, not source. S02 example: forge-completer.md edits (T03/T04 sub-step 1.5/1.6 additions) are live in source but running GSD-WORKER loaded pre-S02 version. This applies generally: any fork-merge agent edits must be followed by re-run of installer to activate in active tasks/milestones. Non-negotiable for deployment timing of agent modifications. Documented in S02-SUMMARY.md Self-Test Observation section.
  source: complete-slice/S02 | updated: 2026-04-16

- [MEM007] (gotcha) confidence:0.85 hits:1 — Compact-signal detection must be first step of dispatch loop. State files (STATE, prefs, AUTO-MEMORY, CODING-STANDARDS) must be re-read fresh after context compaction before deriving next unit. Signal file is deleted immediately after recovery check to avoid re-recovery in next iteration.
  source: execute-task/T03 → complete-slice/S01 | updated: 2026-04-15

- [MEM070] (gotcha) confidence:0.95 hits:0 — Stub regex precedence order is LOCKED and evaluated first-match-wins per line: (1) empty_function_body (2) return_null_function (3) jsx_placeholder_onclick (4) jsx_placeholder_return_div. Order matters because empty_function_body must run first (would trivially match return_null_function in some arrow-function forms). This ordering is canonical and documented in forge-verifier.js comment block lines 15-22. External tools (T02 CLI, T03 wired checker, T05 tests) must respect the same order. Non-negotiable: regex names are LOCKED — S03 VERIFICATION.md references them by name.
  source: execute-task/T01-M003 | updated: 2026-04-18

- [MEM069] (gotcha) confidence:0.95 hits:0 — return_null_function regex flags ALL bare `return null;` lines as stubs — a heuristic that produces false positives on purpose. The bare line is insufficient to distinguish intentional sentinel returns from placeholder stubs; human triages false positives per S03 RISK card. This heuristic is applied even within forge-verifier.js itself (readFileCached line 103 has `return null` that matches the pattern), which is expected behavior demonstrating the heuristic's breadth. Documented in forge-verifier.js lines 70-73 and S03-PLAN.md D2 decision.
  source: execute-task/T01-M003 | updated: 2026-04-18

## Architecture
- [MEM011] (architecture) confidence:0.95 hits:6 — Dispatch template refactoring: standardize all 7 templates to use placeholder substitution (Read-path directives with mandatory vs optional artifacts) instead of inline artifact-reading logic. Templates become thin data-flow descriptors; workers handle all read I/O. Reduces orchestrator prompt size and ensures fresh state per worker. All 7 templates (execute-task, plan-slice, plan-milestone, complete-slice, complete-milestone, discuss-milestone/discuss-slice, research-milestone/research-slice) completed and verified. 24 inlined {content...} placeholders eliminated. forge-auto.md and forge-next.md both use 14-line placeholder substitution block. Note: forge-next preserves a selective memory injection block (lines 123-129) not present in forge-auto — unique structural element.
  source: plan-slice/S02 → research-slice/S02 → execute-task/T01-S02 → execute-task/T02-S02 → execute-task/T03-S02 → complete-slice/S02 → complete-milestone/M001 | updated: 2026-04-15

- [MEM005] (architecture) confidence:0.95 hits:6 — Gradual skill migration: move commands to skills/ with thin shims (6–7 lines), not big-bang refactoring. Shims remain in commands/ pointing to skills/, allowing deprecation over releases. Shims must pass $ARGUMENTS through via Skill({skill:"name", args:"$ARGUMENTS"}) to preserve CLI flags (--skip-brainstorm, --resume, -fast, etc.). Token budgets improve: forge-task now under 5K after migration despite previous concerns. Commands reduced from ~950 total lines to ~20 shim lines after S03 migration (forge-auto, forge-task, forge-new-milestone). All migrated skills follow disable-model-invocation: true convention.
  source: discuss-milestone/M001 → plan-slice/S03 → execute-task/T02-S03 → execute-task/T03-S03 → execute-task/T04-S03 → complete-slice/S03 → complete-milestone/M001 | updated: 2026-04-15

- [MEM048] (architecture) confidence:0.92 hits:2 — Schema documentation embedded in agent frontmatter (agents/forge-planner.md) ensures workers output correctly formatted T##-PLAN with must_haves YAML section. Planner agent now contains verbatim schema example + annotation that "YAML frontmatter is authoritative". Tight coupling between planner instructions and scripts/forge-must-haves.js parser ensures schema contract is discoverable and self-documenting. T##-PLAN output by any future planner will match parseMustHaves expectations. Confirmed in S01 completion: schema lock is enforced, planner T02 edits locked the shape, executor T03 validates against it.
  source: execute-task/T02 → complete-slice/S01 | updated: 2026-04-16

- [MEM016] (architecture) confidence:0.92 hits:2 — Install scripts (install.sh and install.ps1) already have broad glob patterns for copying skills/ — T05 verification only, no new install infrastructure code needed. The existing `skills/**` glob patterns in both installers are sufficient for newly migrated skills. Confirmed: both installers auto-discover new skill directories without modification.
  source: plan-slice/S03 → research-slice/S03 → execute-task/T05-S03 | updated: 2026-04-15

- [MEM003] (architecture) confidence:0.90 hits:2 — /forge REPL router must stay under 5K tokens and survive compaction (no context fallback). Implemented at 126 lines, ~1900 tokens (compact-safe). Minimal dispatch logic only; all substantive work delegated to subagents or skills. Loop re-reads STATE.md after each skill invocation to keep menu fresh despite state mutations.
  source: discuss-milestone/M001 → complete-slice/S03 → complete-milestone/M001 | updated: 2026-04-15

- [MEM044] (architecture) confidence:0.90 hits:1 — Canonical must_haves + expected_output schema LOCKED in M003/S01-PLAN.md (D3 decision): frontmatter keys `must_haves.{truths[], artifacts[{path, provides, min_lines, stub_patterns?}], key_links[{from, to, via}]}` + `expected_output[paths]`. Schema is consumed downstream by S02 (file-audit), S03 (verifier), S04 (plan-checker). All downstream consumers reference same schema shape — no divergence allowed. Lock is documented in S01-PLAN.md § Schema Shape and enforced by T01 parser with CLI validation (exit codes 0/0/2 for legacy/valid/malformed).
  source: plan-slice/S01 → complete-slice/S01 | updated: 2026-04-16

- [MEM041] (architecture) confidence:0.90 hits:1 — Unknown error classification (opaque errorMessage values like "success", "error", "unknown") must be treated as tooling_failure blocker class, not retryable. This is distinct from permanent errors (quota, API key) and provider errors (rate-limit, server, network). Unknown errors surface to user and stop the loop — no retry. Semantic rule for all future retry handler and dispatch template work.
  source: execute-task/T01-S01 → execute-task/T02-S01 | updated: 2026-04-16

- [MEM002] (architecture) confidence:0.90 hits:1 — Lean orchestrator — orchestrator passes artifact paths only, workers read and resolve their own dependencies. This keeps orchestrator prompt under cap and forces workers to fetch fresh state. forge-auto.md Step 3 no longer reads/inlines artifacts directly; instead uses placeholder substitution pattern.
  source: discuss-milestone/M001 → execute-task/T02-S02 | updated: 2026-04-15

- [MEM015] (architecture) confidence:0.85 hits:1 — forge-next.md Step 3 includes selective memory injection block (lines 123-129) that forge-auto.md does NOT have. This is intentional: forge-next is step-by-step, so memory context is injected at each step to keep worker prompt fresh. forge-auto runs full milestone autonomously and injects memory once at entry. The two templates are structurally divergent and must not be merged mechanically.
  source: execute-task/T03-S02 → complete-slice/S02 | updated: 2026-04-15

- [MEM026] (architecture) confidence:0.85 hits:1 — /forge.md and invoked skills must fit within compact-safe token budget: CLAUDE.md (~15K) + /forge.md (<5K) + skill overhead (~2K) = ~22K tokens, within Claude Code re-attachment minimum. This constrains /forge REPL to <5K tokens and forces skill composition to avoid inline context injection. Compact-safe budget is architectural guardrail for post-compaction reliability.
  source: execute-task/T04-S04 → complete-milestone/M001 | updated: 2026-04-15

- [MEM053] (architecture) confidence:0.85 hits:1 — T04 (file-audit in completer) parses `expected_output:` inline via regex rather than extending `scripts/forge-must-haves.js --check` CLI to produce per-task parsed results. Design rationale: keeps S01 schema contract closed (T04 can independently validate without new CLI grammar). Completer shells out to `--check` for presence-only gate (legacy vs valid), but expected_output array parsing stays inline in agent via `node -e`. Preserves separation: CLI is S01 deliverable, S02 consumers adapt to it as-is without asking for new schema outputs. Confirmed implemented in T04 sub-step 1.6.
  source: plan-slice/S02 → execute-task/T04-M003 | updated: 2026-04-16

- [MEM071] (architecture) confidence:0.85 hits:0 — 3-level verification short-circuit rules: Exists fails → Substantive and Wired not evaluated (Wired stays null); Substantive fails → Wired not evaluated (Wired stays null). This ensures that file presence is the first gate, then content quality, then integration checking. Early termination prevents cascading false positives from missing files to wiring errors. Implemented in verifyArtifact (lines 308–345) and documented in forge-verifier.js header comments. Consumers must handle null values for skipped levels per artifact row.
  source: execute-task/T01-M003 | updated: 2026-04-18

## Convention
- [MEM004] (convention) confidence:0.95 hits:3 — All forge-* skills use disable-model-invocation: true. This prevents the skill description from being injected into worker context, saving tokens and reducing noise. Confirmed across forge-auto, forge-task, forge-new-milestone, and all other skills in codebase; no deviations observed.
  source: discuss-milestone/M001 → research-slice/S03 → execute-task/T03-S03 → execute-task/T04-S03 | updated: 2026-04-15

- [MEM049] (convention) confidence:0.90 hits:1 — Evidence Settings section in forge-agent-prefs.md is placed between Verification Settings and Token Budget Settings. Default mode is `lenient`. Section documents all three modes (lenient/strict/disabled) semantics in pt-BR and cross-references S02/S03 consumers. Scaffolded inert until S02 wires consumption. S01 completion confirms: section written in T04, position locked, mode default ready for S02 wiring.
  source: execute-task/T04 → complete-slice/S01 | updated: 2026-04-16

- [MEM065] (convention) confidence:0.90 hits:1 — File Audit Settings section in forge-agent-prefs.md is placed between Evidence Settings and Token Budget Settings. Section documents file_audit.ignore_list default array (7 patterns: lockfiles + dist/build/.next/.gsd). Follows exact style of Evidence Settings section (pt-BR intro, fenced config block, Semântica subsection, Cross-references subsection). Downstream consumer: forge-completer T04 file-audit diff validation. Placement position locked to maintain topical grouping of advisory-pipeline prefs. Confirmed cascaded in T04.
  source: execute-task/T05-M003 → execute-task/T04-M003 | updated: 2026-04-16

- [MEM024] (convention) confidence:0.90 hits:1 — CHANGELOG.md versioning places new major versions ABOVE existing entries (v1.0.0 above v0.7.3). Follows reverse chronological order for human readability. Critical for release planning tasks that append CHANGELOG entries before tagging.
  source: research-slice/S04 → execute-task/T01-S04 | updated: 2026-04-15

- [MEM027] (convention) confidence:0.90 hits:1 — Release strategy: stage and commit all milestone changes in one batch (feat: commit with full changelog) before creating annotated tag. This keeps git history clean — the tag points to a single consolidated commit, not scattered across incremental commits. Confirmed in M001 v1.0.0 release: 13 files, 1375 insertions staged together, then tagged.
  source: execute-task/T03-S04 → complete-slice/S04 | updated: 2026-04-15

- [MEM028] (convention) confidence:0.90 hits:1 — CHANGELOG.md entries enriched from slice-level summaries (S##-SUMMARY.md), not from ROADMAP drafts or M##-SUMMARY aggregates. Summaries contain implementation-accurate descriptions of Features, Breaking Changes, and Architecture decisions. This produces release notes that reflect actual deliverables, not intended scope.
  source: execute-task/T01-S04 → complete-milestone/M001 | updated: 2026-04-15

- [MEM042] (convention) confidence:0.90 hits:0 — Error classifier output shape is {kind, retry, backoffMs}. The `retry` field is an explicit boolean (always true for transient kinds 1–5, always false for permanent/unknown). Field `backoffMs` is OMITTED when retry=false, reducing JSON noise. Kind values: "permanent"|"rate-limit"|"network"|"stream"|"server"|"connection"|"unknown". Ported from GSD-2 {kind, retryAfterMs} — Forge spec renamed retryAfterMs → backoffMs and added explicit retry boolean for clarity. Contract locked in scripts/forge-classify-error.js.
  source: execute-task/T02-S01 | updated: 2026-04-16

- [MEM054] (convention) confidence:0.90 hits:0 — Perf budget semantics in task constraints: "≤15 ms p50 / ≤50 ms p95" is ADDITIONAL latency cost vs baseline (no hook), not absolute end-to-end time. Node cold-start on Windows (first tool call per session) dominates; warm disk append is 2–5 ms. Hook budget is relative to its own work, not tool execution time. Task constraint documents measurement unit as "perf budget," not "total latency budget."
  source: plan-slice/S02 | updated: 2026-04-16

- [MEM025] (convention) confidence:0.85 hits:1 — README.md primary entry point is /forge REPL command. Documentation strategy: /forge appears first in Quick start and commands table; /forge-auto and /forge-new-milestone are annotated as aliases that delegate to /forge. This clarifies the command hierarchy and guides users to the main navigation interface. Reflects shift toward integrated command REPL as primary UX.
  source: research-slice/S04 → execute-task/T02-S04 | updated: 2026-04-15

- [MEM072] (convention) confidence:0.90 hits:0 — Module exports pattern for verifier-like tools: main API function (e.g., verifyArtifact), canonical library export (e.g., DEFAULT_STUB_REGEXES), and _private object exposing internal helpers (checkExists, checkSubstantive, readFileCached) for downstream test suites. The _private export is explicitly named to signal semi-internal API (not officially supported but available for tight-coupling tests). Dependency: verifyArtifact requires parseMustHaves from forge-must-haves.js, loaded unconditionally to avoid re-requiring by T02/T03. Pattern scales as S03/S04 tools emerge with similar needs.
  source: execute-task/T01-M003 | updated: 2026-04-18

## Environment
- [MEM023] (environment) confidence:0.90 hits:2 — forge-statusline.js version is resolved at runtime via `git describe --tags --always` (line 162), not hardcoded. This enables automatic version tracking across releases without code edits. Tag annotation (must be annotated, not lightweight) is created separately (via T03 in release planning). Latest tag is v0.25.0 with 3 commits after.
  source: plan-slice/S04 → research-slice/S04 → complete-slice/S04 | updated: 2026-04-15

## Pattern
- [MEM020] (pattern) confidence:0.95 hits:1 — REPL loop re-reads STATE.md after each skill returns to keep status line fresh. Skills modify milestone/slice/task state, and without re-read, subsequent menu display shows stale progress. /forge REPL menu confirmed this pattern. Applies to all persistent REPL loops in Forge.
  source: execute-task/T01-S03 → complete-slice/S03 | updated: 2026-04-15

- [MEM031] (pattern) confidence:0.90 hits:1 — Deterministic Node CLI utility pattern (module export + dual stdin/--msg CLI mode) separates data transformation from dispatch logic. Pure functions with structured JSON output (`{kind, retry, backoffMs}` for classifiers, `{legacy, valid, error}` for schema detectors) enable reuse across multiple consumers. Examples: scripts/forge-classify-error.js (error classification), scripts/forge-must-haves.js (schema detection for S03/S04). Pattern scales as new analysis tasks emerge.
  source: plan-slice/S01 → plan-slice/S01 | updated: 2026-04-16

- [MEM045] (pattern) confidence:0.90 hits:1 — Schema detection predicate pattern: centralized `hasStructuredMustHaves(content)` boolean in `scripts/forge-must-haves.js` returns true IFF frontmatter contains root-level `must_haves:` key. Both dual-mode (module export + CLI --check) and dual-result (legacy vs structured) enable reuse by S03 verifier and S04 plan-checker without code duplication. Predicate is I/O-free and deterministic — suitable for embedding in workers as shell-out validation gate. Confirmed implementation: 370 lines, 29/29 smoke tests, includes parseMustHaves for full schema extraction.
  source: plan-slice/S01 → execute-task/T01 | updated: 2026-04-16

- [MEM061] (pattern) confidence:0.90 hits:0 — Evidence JSONL structure: one-line-per-tool-call append to `.gsd/forge/evidence-{unitId}.jsonl`, each line ≤512 bytes, fields {ts, tool, cmd, file, ok, interrupted}. Field semantics: `ok:boolean` (derivable from stderr presence), `tool:string` (Bash|Write|Edit), `cmd` and `file` subject to progressive truncation to fit byte budget. Append pattern implies idempotent writes — hook may be invoked multiple times per tool call, so evidence line should be written at most once per call. Pattern established in T01 as foundation for S02 file-audit and S03 verification consumers. Non-negotiable: ts and tool fields never truncated; cmd shrinks before file shrinks; unit_id is part of filename, not line content.
  source: execute-task/T01-M003 | updated: 2026-04-16

- [MEM062] (pattern) confidence:0.90 hits:0 — Unit ID resolution from auto-mode.json worker field: split on `/` and take right half (S##/T## → T##). Malformed worker or absent field → fallback 'adhoc'. String operation (no parsing), enables orphaned task detection (T## at root = 'adhoc', never part of slice tracking). Applied in T01 `resolveUnitId` helper for evidence filename generation. Idempotent: invoking same task multiple times re-uses same evidence file (append-only).
  source: execute-task/T01-M003 | updated: 2026-04-16

- [MEM063] (pattern) confidence:0.90 hits:1 — Executor frontmatter contract: `verification_evidence:` field (array of `{command, exit_code, matched_line}` objects) is emitted in T##-SUMMARY.md frontmatter, separate from JSONL evidence log structure (MEM061). Empty array `verification_evidence: []` is valid when evidence log is unavailable. Sentinel `matched_line: 0` indicates `grep -n -m 1 -F` found no match; non-zero matched_line points to line number in log (1-indexed). Documented in agents/forge-executor.md step 12a. Completer cross-references these claims against `.gsd/forge/evidence-{T##}.jsonl` in sub-step 1.5 (before File Audit); three mismatch conditions (command_not_in_log, command_mismatch_at_line, evidence_log_missing) produce advisory Evidence Flags table. Executor must always emit verification_evidence field even when empty (required for schema contract compliance).
  source: execute-task/T02-M003 → execute-task/T03-M003 | updated: 2026-04-16

- [MEM073] (pattern) confidence:0.90 hits:0 — Per-invocation file cache pattern: Map cleared at verifyArtifact() entry (line 273), preserving freshness across separate invocations while avoiding redundant filesystem I/O within a single verification run. readFileCached returns null for ENOENT (cached), rethrows other errors (permission, EIO). Cache key is absolute path; readFileCached resolves relative paths via path.join(cwd, artifactPath). Idempotent invocation safety: calling verifyArtifact twice on same artifacts produces two independent cache maps, preventing stale inter-invocation state. Pattern suitable for I/O-intensive graph traversal (key-link resolution in T03) where same file may be referenced multiple times.
  source: execute-task/T01-M003 | updated: 2026-04-18

- [MEM074] (pattern) confidence:0.90 hits:0 — CLI argv flag-loop pattern for tools with optional parameters: parseArgv iterates through argv array, checking each arg against known flags (--slice, --milestone, --cwd, --help) with lookahead to consume next arg as value if present. Silent skip for unknown flags. Implemented in scripts/forge-verifier.js lines 369–379 with early returns per flag to avoid redundant comparisons. Pattern applies to dual-mode utilities (module export + CLI entrypoint via require.main check). Confirmed in T02: parseArgv, discoverTaskPlans, aggregateMustHaves, runSliceVerification invoked sequentially by CLI dispatch block (lines 659–681).
  source: execute-task/T02-M003 | updated: 2026-04-18

- [MEM075] (pattern) confidence:0.90 hits:0 — 4-bucket plan aggregation with non-fatal isolation: structured (valid schema), legacy (no must_haves: key), malformed (schema threw parse error), errors (file I/O failed). Each bucket processed independently; malformed and error buckets produce single skip-row per plan, allowing other plans in slice to continue. aggregateMustHaves gate (line 426) checks hasStructuredMustHaves before calling parseMustHaves (MEM051 try/catch pattern). Result: slices with mixed plan types (e.g., legacy T01 + structured T02 in same slice) process successfully without cascading failures. Pattern scales to S03/T03 (Wired checking) and S03/T05 (test suite generation). Containers: four-field result object { structured, legacy, malformed, errors }; rows for non-structured plans omit exists/substantive/wired fields (set to null). Documented in T02-SUMMARY.md.
  source: execute-task/T02-M003 | updated: 2026-04-18

- [MEM076] (convention) confidence:0.85 hits:0 — writeVerificationMd function signature takes sliceId as explicit 3rd parameter (not derived from sliceDir basename). Design allows caller to control output filename independently of directory structure. Function signature: writeVerificationMd(sliceDir: string, sliceId: string, md: string) → string (path written). Implemented in scripts/forge-verifier.js line 652. Upstream invoker (runSliceVerification caller, T02 CLI dispatch) passes opts.slice as the sliceId. Non-negotiable for output path clarity — enables future refactoring of directory naming without breaking output contracts. Evaluated by T03 (Wired checking) and later S03 consumers without modification.
  source: execute-task/T02-M003 | updated: 2026-04-18

- [MEM077] (architecture) confidence:0.85 hits:0 — Wired checker uses BFS (breadth-first search) rather than DFS for import-chain depth tracking in depth-2 walker. BFS simplifies max_depth accounting and preserves distance invariant across branches. Key components: SUPPORTED_EXTENSIONS (frozen list: .js/.ts/.tsx/.jsx/.mjs/.cjs), extractImports regex set for ESM/CJS/re-export patterns, resolveSpec tries extensions in SUPPORTED_EXTENSIONS order (most common first: .js → .ts → .tsx, etc.), walkImports returns {found, depth, visited} with metadata for debugging approximate verdicts. Walker reuses S01 file cache for efficiency; bare package specs (lodash, react) skipped intentionally (external scope). Wired verdict changes from null to true/false/approximate/skipped — approximate when depth limit hit (walker bottomed out), skipped when non-JS/TS repo. Non-negotiable: resolver order is locked (most common extensions first), and walker_info metadata is essential for S03 consumers to assess verification confidence in mixed-content repos.
  source: execute-task/T03-M003 | updated: 2026-04-18
